authentication {
    jwt {
        audience = "test-audience"
        issuer = "test-issuer"
        realm = "test-realm"
        secret = "test-secret"
        authExpirationInMillis = "10000"
    }
}

cors {
    allowedHosts = [ "test-host-1:3000", "test-host-2:3000" ]
}